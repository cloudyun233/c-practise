// 补充题 1 递归实现汉诺塔（覆盖 2019 年真题递归考点）
// 题目内容：有三根柱子 A、B、C，A 柱上有 n 个大小不同的圆盘，大的在下，小的在上。要求将 A 柱上的所有圆盘移动到 C 柱，每次只能移动一个圆盘，且任何时刻都不能将大圆盘放在小圆盘上，B 柱可作为辅助柱。请编写程序，读入正整数 n，输出所有的移动步骤，格式为源柱子->目标柱子。
//
// 输入格式：一个正整数 n，表示圆盘的个数，1<=n<=10。
//
// 输出格式：每行输出一个移动步骤，格式为X->Y，无多余输出。
//
// 输入样例：
// 3
//
// 输出样例：
// A->C
// A->B
// C->B
// A->C
// B->A
// B->C
// A->C













// 补充题 2 函数指针实现梯形法求定积分（覆盖 2018 年真题改错题核心考点）
// 题目内容：请编写程序，使用梯形法计算定积分∫(a到b) f(x)dx，要求通过函数指针传入被积函数，分别计算以下两个定积分：
// f(x) = 1 + x³，积分区间 [-1, 1]
// f(x) = x²，积分区间 [0, 2]
// 将区间 [a,b] 等分为 100 份，用梯形法计算积分值，最终输出两个积分结果，保留 6 位小数。
//
// 输入格式：无输入。
//
// 输出格式：两行，第一行输出第一个积分结果，第二行输出第二个积分结果，均保留 6 位小数。
//
// 输出样例：
// 2.000000
// 2.666667
