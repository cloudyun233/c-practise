/*
================================================================================
                         C++ 编程练习题（按视频顺序进阶）
================================================================================
说明：每道题覆盖 3-5 个连续视频知识点，难度循序渐进。请在代码中
      实现需求，并通过注释标注关键知识点的应用位置。
================================================================================
*/

// ============================================================================
// 【编程题 5】Const 与 Mutable："温度传感器"模拟器
// 覆盖视频：33（Const）、34（Mutable）、10（Header Files）、17（References）、21（Static）
// ----------------------------------------------------------------------------
// 【功能需求】
// 1. 创建 `TemperatureSensor` 类：
//    - 私有成员：
//        double currentTemp;       // 当前温度
//        mutable int readCount;    // 读取次数（即使在 const 函数中也能修改）
//        static double maxTemp;    // 历史最高温（静态）
//    - 公有构造函数：初始化温度，readCount 置 0，更新 maxTemp。
//    - 公有成员函数：void setTemp(double t); // 设置温度，更新 maxTemp。
//    - 公有成员函数：double getTemp() const; // 返回温度，同时 readCount++（需 mutable）。
//    - 公有静态成员函数：static double getMaxTemp(); // 返回历史最高温。
// 2. 在 main 中：
//    - 创建一个 const 对象和一个非 const 对象，测试 getTemp() 的 const 特性。
//    - 观察 readCount 和 maxTemp 的变化。
//
// 【技术限制】
// - getTemp() 必须是 const 成员函数。
// - readCount 必须用 mutable 修饰。
// - maxTemp 必须是静态成员，由所有对象共享。
//
// 【预期效果示例】
// 传感器 1 当前温度：25.5 (读取次数: 1)
// 历史最高温：30.0
// const 传感器温度：20.0 (读取次数: 1)
// ============================================================================

// 在这里编写你的代码（或在此处预留代码框架，用注释提示关键实现点）
