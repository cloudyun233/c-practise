/*
================================================================================
                         C++ 编程练习题（按视频顺序进阶）
================================================================================
说明：每道题覆盖 3-5 个连续视频知识点，难度循序渐进。请在代码中
      实现需求，并通过注释标注关键知识点的应用位置。
================================================================================
*/

// ============================================================================
// 【编程题 6】成员初始化列表与 mutable："学生成绩管理类"
// 覆盖视频：33（Const）、34（Mutable）、36（成员初始化列表）、37（三元操作符）、38（对象生存期）、42（this指针）
// ----------------------------------------------------------------------------
// 【功能需求】
// 1. 设计一个 Student 类，包含以下功能：
//    - 使用成员初始化列表初始化学生姓名（std::string）和学号（int）。
//    - 包含一个 mutable 成员变量 visit_count，在 const 成员函数 ShowScores() 中每次调用时递增该计数。
//    - 成绩存储在 std::vector<int> 中，提供 AddScore(int score) 函数添加成绩（添加前使用 reserve() 优化空间）。
//    - 在 ShowScores() 中使用三元操作符判断成绩是否为空，为空则输出"暂无成绩"，否则输出成绩列表。
//    - 使用 this 指针区分成员变量与参数名。
//    - 在 main() 中测试栈对象的生存期。
//
// 【技术限制】
// - 必须使用成员初始化列表初始化成员变量。
// - ShowScores() 必须是 const 成员函数。
// - visit_count 必须用 mutable 修饰。
// - 使用 this-> 访问成员变量。
//
// 【预期效果示例】
// 学生姓名: 张三, 学号: 1001
// 暂无成绩
// 成绩列表 (访问次数: 1): 85 90 78
// 成绩列表 (访问次数: 2): 85 90 78
// ============================================================================

// 在这里编写你的代码（或在此处预留代码框架，用注释提示关键实现点）
