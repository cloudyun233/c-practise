/*
================================================================================
                         C++ 编程练习题（按视频顺序进阶）
================================================================================
说明：每道题覆盖 3-5 个连续视频知识点，难度循序渐进。请在代码中
      实现需求，并通过注释标注关键知识点的应用位置。
================================================================================
*/

// ============================================================================
// 【编程题 4】继承与多态："形状"面积计算器
// 覆盖视频：26（Inheritance）、27（Virtual Functions）、28（Interfaces）、29（Visibility）、31（Strings）
// ----------------------------------------------------------------------------
// 【功能需求】
// 1. 定义一个抽象基类 `Shape`（接口），包含：
//    - 保护成员：string name; // 形状名称
//    - 公有纯虚函数：double getArea() const = 0; // 计算面积
//    - 公有虚函数：void printInfo() const; // 打印名称和面积（可被子类覆盖）
// 2. 定义 `Circle` 类，公有继承 `Shape`：
//    - 私有成员：double radius;
//    - 构造函数：初始化半径，并设置 name 为 "Circle"。
//    - 重写 getArea()：返回 π * r² (π 取 3.14159)。
// 3. 定义 `Rectangle` 类，公有继承 `Shape`：
//    - 私有成员：double width, height;
//    - 构造函数：初始化宽高，并设置 name 为 "Rectangle"。
//    - 重写 getArea()：返回宽 * 高。
// 4. 在 main 中：
//    - 创建 Shape* 数组，包含一个 Circle 和一个 Rectangle。
//    - 循环调用 printInfo()，演示多态。
//
// 【技术限制】
// - 基类必须包含纯虚函数（抽象类）。
// - 必须通过基类指针/引用调用子类函数（运行时多态）。
// - 注意继承的访问权限（public 继承）。
//
// 【预期效果示例】
// Shape: Circle, Area: 78.53975
// Shape: Rectangle, Area: 20
// ============================================================================

// 在这里编写你的代码（或在此处预留代码框架，用注释提示关键实现点）
