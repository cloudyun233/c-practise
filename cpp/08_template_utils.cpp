/*
================================================================================
                         C++ 编程练习题（按视频顺序进阶）
================================================================================
说明：每道题覆盖 3-5 个连续视频知识点，难度循序渐进。请在代码中
      实现需求，并通过注释标注关键知识点的应用位置。
================================================================================
*/

// ============================================================================
// 【编程题 8】模板与命名空间："模板工具函数集"
// 覆盖视频：53（模板）、52（多返回值）、48（std::vector优化）、60（命名空间）
// ----------------------------------------------------------------------------
// 【功能需求】
// 1. 在自定义命名空间 MyUtils 中实现以下模板函数：
//    - 模板函数 Sort：接受 std::vector<T> 和函数指针 bool (*compare)(T, T) 作为比较规则，实现排序（可使用冒泡排序或标准库 std::sort）。
//    - 模板函数 GetStats：接受 std::vector<T>，返回 std::tuple<T, T, double>（最大值、最小值、平均值），平均值使用 static_cast 转换为 double。
// 2. 在 main() 中测试：
//    - 对 std::vector<int> 升序/降序排序（通过函数指针自定义规则）。
//    - 统计浮点数向量的最大值、最小值和平均值。
//
// 【技术限制】
// - 必须定义在自定义命名空间 MyUtils 中。
// - Sort 函数必须接受函数指针作为参数。
// - GetStats 必须返回 std::tuple。
// - 使用 static_cast 进行类型转换。
// - 使用 reserve() 预分配 vector 空间。
//
// 【预期效果示例】
// 排序前: 5 2 8 1 9
// 升序排序: 1 2 5 8 9
// 降序排序: 9 8 5 2 1
// 统计: max=9.5, min=2.1, avg=5.2
// ============================================================================

// 在这里编写你的代码（或在此处预留代码框架，用注释提示关键实现点）
